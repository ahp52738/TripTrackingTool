<!DOCTYPE html>
<html>
  <head>
    <title>TripManagementApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
     <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
        <div style="position: absolute; top: 10px; right: 10px;">
          <% if user_signed_in? %>
            <div style="color: #333; margin-right: 10px;">Welcome::: <%= current_user.email %></div>
            <%= button_to "Sign out", destroy_user_session_path, method: :delete, class: "red-button" %>
          <% end %>
        </div>


    <%= yield %>
  </body>
</html>
